/* Button defaults to width: 100% with a 200px minimum, which is sized for a
 * form footer that spans the dialog.  In a card footer it only needs to be
 * as wide as its label. */
.actionButton {
    width: auto;
    min-width: 0;
}
