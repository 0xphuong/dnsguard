.menu {
    padding-top: 8px;
    display: none;
    /* --main-box-shadow is a colour; a lone colour is invalid here. */
    box-shadow: var(--elev-2);
    min-height: 100vh;
    max-width: 240px;
    flex-shrink: 0;
    z-index: 1;

    @media (min-width: 1200px) {
        display: flex;
        flex-direction: column;
    }
}

.menuWrapper {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    min-height: calc(100vh - 80px);
    padding-bottom: env(safe-area-inset-bottom);

    @media (min-width: 1200px) {
        min-height: 0;
        flex: 1;
    }

    &.headerMenu {
        min-height: 100%;
        justify-content: flex-start;
        gap: 80px;
    }
}

.menuLink,
.accordionLink {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px 16px;
    font-size: 16px;
    line-height: 21px;
    font-weight: var(--weight-regular);
    color: var(--sidebar-text);
    text-decoration: none;
    max-width: 100%;
    white-space: nowrap;
    text-overflow: ellipsis;
    overflow: hidden;
    vertical-align: middle;
    transition:
        background-color var(--t2),
        color var(--t2);
    outline: none;

    &:hover,
    &:focus {
        color: var(--sidebar-text-active);
        background-color: var(--sidebar-hover-background);
        text-decoration: none;
    }

    /* Active is near-white on a darker ground rather than the brand hue:
     * cyan on slate-900 is the one place the link colour cannot be read. */
    &.activeLink {
        color: var(--sidebar-text-active);
        background-color: var(--sidebar-hover-background);

        .linkIcon {
            color: var(--sidebar-text-active);
        }
    }
}

.accordionLink {
    padding: 12px 16px 12px 52px;
}

.linkIcon {
    color: var(--sidebar-icon);
    transition: color var(--t2);
}

.withConfirmIcon {
    display: flex;
    align-items: center;
}

.topMenuWrapper {
    margin-bottom: auto;

    @media (min-width: 1200px) {
        margin-bottom: 0;
    }
}

.referenceWrapper {
    margin-top: auto;
}

.accordionHeader {
    cursor: pointer;
    user-select: none;
}

.accordionArrow {
    margin-left: auto;
    color: var(--sidebar-icon);
    transition:
        transform var(--t2),
        color var(--t2);
}

.accordionArrowOpen {
    transform: rotate(180deg);
}
